●  1. Aim
To implement a program that, given an undirected graph with n nodes and a list of edges, determines whether a valid path exists between a given source and destination node. The solution builds an adjacency list and runs a breadth-first search (BFS) in O(V + E) time and O(V + E) space.

